apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rosette-server.fullname" . }}-test-connection"
  labels:
    {{- include "rosette-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rosette-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
